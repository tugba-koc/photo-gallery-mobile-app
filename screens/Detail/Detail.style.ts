import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  header_container: {
    flexDirection: 'row',
    width: '90%',
    alignItems: 'center',
    columnGap: 14,
    paddingHorizontal: 20,
  },
});
