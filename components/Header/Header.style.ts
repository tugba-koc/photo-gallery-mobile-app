import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    color: 'black',
    fontWeight: '600',
    width: '100%',
    backgroundColor: 'white',
    padding: 14,
    borderRadius: 20,
  },
  search_section: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
