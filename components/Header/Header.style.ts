import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    color: 'black',
    fontWeight: '600',
    width: '100%',
    backgroundColor: 'white',
    padding: 10,
    borderRadius: 20,
  },
  searchSection: {
    display: 'flex',
    alignItems: 'center',
    marginTop: 60,
  },
});
